# AGENT Instructions

- Run `npm test` before each commit.
- Keep the repository root tidy; remove stray files.
- Document progress in `PROGRESS.md` after each session.
- Do not install `xvfb` or `wine`; Windows builds run in GitHub Actions.
